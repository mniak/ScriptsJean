{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "createdirs",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/createdirs"
        }
        
    ]
}